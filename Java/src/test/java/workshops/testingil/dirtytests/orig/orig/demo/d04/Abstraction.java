package workshops.testingil.dirtytests.orig.orig.demo.d04;

import static org.junit.jupiter.api.Assertions.assertTrue;

import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;

import org.junit.jupiter.api.Test;

class Abstraction {
/*
	private String lyrics;

	@Test
	void noisy_test() throws Exception {
		Path path = Paths.get("BackInBlackLyrics.txt");
		lyrics = new String(Files.readAllBytes(path));
		assertTrue(lyrics.contains("I'm back in black"));
	}

	@Test
	void readable_test() throws Exception {
		readLyrics("BackInBlackLyrics.txt");
		shouldContain("I'm back in black");
	}

	private void readLyrics(String filename) throws Exception {
		Path path = Paths.get("BackInBlackLyrics.txt");
		lyrics = new String(Files.readAllBytes(path));
	}

	private void shouldContain(String line) {
		assertTrue(lyrics.contains(line));
	}
*/
}
